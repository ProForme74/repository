<div role="main" class="ui-content">
    <div data-role="collapsible-set">
      <div data-role="collapsible" data-collapsible="false">
        <h3>Intenzivna</h3>
        <ul>
          <% for(var item in items) { %>
              <li class="btn btn-info " style="margin-top: 5px;"><%=items[item].šifra%></li> 
              <li class="btn btn-info " style="margin-top: 5px;"><%=items[item].status%></li> <br>
              <% } %>
      </ul>
      
      </div>
      
    </div>
</div>

<form action="/krevet" method="POST">
  
  
  <div class="form-group">
    <label >Izmjeni status kreveta: </label>
    <input
      type='text'
      id="krevet"
      name="krevet"
      class="form-control"
    />
    
    <input
      type="text"
      id="status"
      name="status"
      class="form-control"
      hidden
    />
    
  </div>
  <button type="submit" class="btn btn-success">
    Unesi
  </button>
</form>

<br>
<a href=".." class="btn btn-secondary">Nazad</a>

<script>
    console.log("OVO");
    var elements = document.getElementsByTagName('li');
    for (var i=0; i<elements.length; i++) {
        console.log(elements[i].innerHTML);
        if((elements[i].innerHTML.length)<3){
          if((elements[i+1].innerHTML)=="true"){
            elements[i].classList.add("slobodan");
            elements[i+1].classList.add("slobodan");
            elements[i+1].innerHTML ="slobodan";
            
          } else {
            elements[i+1].innerHTML ="zauzet";
          }
        }
    }
    document.getElementById("krevet").onchange = function(){
      var krevet=document.getElementById("krevet").value;
      console.log(krevet);
      for (var i=0; i<elements.length; i++) {
        //console.log(elements[i].innerHTML);
        if(elements[i].innerHTML==krevet){
          console.log(elements[i].innerHTML);
          if((elements[i+1].innerHTML)=="slobodan"){
            document.getElementById("status").value="slobodan";
          }else{
            document.getElementById("status").value="zauzet";
          }
        }
    }
    }
    
    
</script>
  <style>
    .slobodan{
      background-color: purple;
    }
</style>
  